# @opensyntaxhq/autodocs-ui

React UI for Autodocs. This package is built and bundled by the CLI and is not intended for direct use.

## Development

```bash
npm run dev
npm run build
npm run test
```

## Notes

The UI assets are copied into the generated `docs-dist` folder during `autodocs build`.
